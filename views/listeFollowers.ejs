<%- include('component/header') %>
<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <% for(follower of followers){ %>
                <h4><%= follower.User.username %></h4>
            <% } %>
        </div>
    </div>
</div>